


<div id="siteContainer">
	<div class="alltheInfo">


		<h2>These are the <%= link_to "AudioMack", 'http://www.audiomack.com/' %> links for today</h2><br />
		
		<!--
		Make it link to a loud hip hop page that will scrape the download links from the top results. 
		-->
	<% @jam.each do |x, i| %>
	<div class="square3">
	<a href="http://www.audiomack.com<%= x.attr('href').to_s %>"><img src="<%= i.attr('src').to_s %>"/></a><br />
	<%= x.text %><br />
	<% link = 'http://www.google.com/search?q=' + x.text.delete("&").gsub(/\s+/, "+") + '+download' %>
		<a href=<%= link %> target="_blank">Download</a>
	<% link1 = 'https://www.youtube.com/results?search_query=' + x.text.delete("&").gsub(/\s+/, "+") %><br />
		<a href=<%= link1 %> target="_blank">Youtube</a><br />
	</div>
	<% end %>
			
	<% @trending_hash.each do |x, i| %>
	<div class="square3">
	<a href="http://www.audiomack.com<%= x.attr('href').to_s %>"><img src="<%= i.attr('src').to_s %>"/></a><br />
	<%= x.text %><br />
	<% link = 'http://www.google.com/search?q=' + x.text.delete("&").gsub(/\s+/, "+") + '+download' %>
		<a href=<%= link %> target="_blank">Download</a>
	<% link1 = 'https://www.youtube.com/results?search_query=' + x.text.delete("&").gsub(/\s+/, "+") %><br />
		<a href=<%= link1 %> target="_blank">Youtube</a><br />
	</div>
	<% end %>

	</div>
</div>


